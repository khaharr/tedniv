{% extends 'base.html.twig' %}

{% block title %}{{ article.NomDeLarticle }}{% endblock %}

{% block body %}
    <h1>{{ article.NomDeLarticle }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ article.NomDeLarticle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article.Description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ article.Prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestion_aticle') }}">back to list</a>

    {#<a href="{{ path('app_puppy_edit', {'id': puppy.id}) }}">edit</a>#}

    {#{{ include('puppy/_delete_form.html.twig') }}#}
{% endblock %}