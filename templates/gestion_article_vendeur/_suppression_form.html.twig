<form method="post" action="{{ path('app_article_suppression', {'id': article.id}) }}" onsubmit="return confirm('Are you sure about that?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button class="btn">supprimer</button>
</form>